var celsius = prompt("Enter the temperature in Celcius", "celsius");

function convertTemperature (celsius) {
    var fahrenheit;
    
    // Do not modify the code above.
    // Convert Celcius to Fahrenheit below. Fahrenheit = (Celcius * 9/5) + 32
        
    
    // Do not modify the code below.
    return fahrenheit
}

document.getElementById("celsius").textContent = celsius;
document.getElementById("fahrenheit").textContent = convertTemperature(celsius);
